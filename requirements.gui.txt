# gujitools GUI runtime and packaging dependencies
# Install with: python -m pip install -r requirements.gui.txt
#
# build.py 会把本文件装进 yolobuild 构建环境：CLI 与 GUI 打进同一个目录
# （共享 _internal），所以构建环境必须同时具备 CLI 与 GUI 的全部依赖。
# 因此这里也列了 ruamel.yaml（CLI 初始化模板用）等共享运行时依赖。

# Desktop UI
PySide6>=6.6.0
PySide6-Fluent-Widgets>=1.5.0   # import qfluentwidgets

# PDF preview and extract
PyMuPDF>=1.23.0

# Image preview and output
Pillow>=9.0.0
numpy>=1.21.0

# Configuration and PDF generation
PyYAML>=6.0
fpdf2>=2.7.0
ruamel.yaml

# crop / rembg / cropremove runtime
opencv-python-headless>=4.5.0
ultralytics>=8.0.0

# GUI packaging
pyinstaller>=6.0.0
